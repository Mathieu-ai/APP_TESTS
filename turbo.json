{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "dev": {
            "cache": false
        },
        "format": {
            "dependsOn": ["^build"],
            "outputs": []
        },
        "build": {
            "outputs": [".next/**", "build/**", "dist/**"]
        }
    }
}
